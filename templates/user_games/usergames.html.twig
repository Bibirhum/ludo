
<link href="https://fonts.googleapis.com/css?family=Catamaran:100,200,300,400,500,600,700,800,900" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css?family=Lato:100,100i,300,300i,400,400i,700,700i,900,900i" rel="stylesheet">
  <link href="{{ asset('landing_page/css/addedNewGameUser.css') }}" rel="stylesheet">

<div class="example-wrapper mx-auto w-50">
    <h1>Ajout à ma ludothèque</h1>

    <p class="message">{{ message }}</p>
    {# <p>Joueur :  {{ user.username }}</p> #}

    <p>Jeu : {{ game.name }}</p>

    <img src="{{ game.image }}">

    <p>Association :<br>
        {# id : {{ user_game.id }}<br> #}
        Commentaire : {{ user_game.commentary }}<br>
        Note : {{ user_game.rating }}<br>

        <br>
        <br>
        {% if user_game.playsGame is same as (1)  %}
        <i class="far fa-thumbs-up"></i>
                    J'y joue !
                    {% endif %}
                    {% if user_game.ownsGame is same as (1) %}
                    <i class="far fa-hand-paper"></i>
                    </i>Je l'ai !
                    
                    {% endif %} 
                    <br>
                    <br>
                    <button class="btn btn-lg btn-primary btn--animated" type="submit" onclick="location.href='{{ path('user_profile') }}'">
                Voir mon profil
            </button>
    </p>
</div>


 <script src="https://kit.fontawesome.com/cef3058f65.js"></script>
